<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.cd.dao.SmbmsBillDao">
    <resultMap id="bill" type="org.cd.entity.SmbmsBill">
        <result property="billCode" column="billCode"></result>
        <result property="productName" column="productName"></result>
        <result property="proName" column="proName"></result>
        <result property="totalPrice" column="totalPrice"></result>
        <result property="isPayment" column="isPayment"></result>
        <result property="creationDate" column="creationDate"></result>
    </resultMap>
    <select id="showSmbmsBills" resultMap="bill">
        select * from smbms_bill
        <where>
            <if test="name !=null">
                productName like concat("%",#{name},'%')
            </if>
        </where>
    </select>
</mapper>